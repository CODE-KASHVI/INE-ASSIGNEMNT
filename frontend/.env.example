# Copy to .env for local dev. On Vercel, set this as a project environment variable instead.
# Must point at the deployed backend's /api root (no trailing slash).
VITE_API_URL=http://localhost:5000/api
